void launchMessengerOauth(String url) {
  throw UnsupportedError(
    'Messenger OAuth launcher is only available on web. Mobile flow not yet implemented.',
  );
}

String currentOrigin() {
  throw UnsupportedError(
    'currentOrigin is only available on web.',
  );
}
