import 'dart:async';

class RepositoryModule {
  static Future<void> configureRepositoryModuleInjection() async {
    // repository:--------------------------------------------------------------
    // Register repository implementations here as features are added.
    // Example:
    // getIt.registerSingleton<FeatureRepository>(
    //   FeatureRepositoryImpl(getIt<FeatureApi>()),
    // );
  }
}
